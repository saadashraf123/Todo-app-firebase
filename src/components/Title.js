import React from 'react'
import { useSelector } from 'react-redux'

const Title = () => {
    return (
        <div className='title'>
            <h2>Todo App Firebase</h2>
        </div>
    )
}

export default Title